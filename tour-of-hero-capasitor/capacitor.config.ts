import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angularprojects.tourofherocapasitor',
  appName: 'tour-of-hero-capasitor',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
